source 'https://rubygems.org'

gemspec

%w[
  rspec
  rspec-core
  rspec-expectations
  rspec-mocks
  rspec-support
].each do |library|
  gem library, :git => "https://github.com/rspec/#{library}.git"
end
